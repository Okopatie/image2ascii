# image2ascii

A command line tool to convert images to ascii art.

Created in rust.

## Usage

```
image2ascii [OPTIONS] <PATH>

Arguments:
  <PATH>  

Options:
  -s, --size <WIDTH> <HEIGHT>  
  -h, --help                   Print help
  -V, --version                Print version
```
